<nav class="relative bg-gray-800/50 backdrop-blur-sm after:pointer-events-none after:absolute after:inset-x-0 after:bottom-0 after:h-px after:bg-white/10">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 items-center justify-between">

            <div class="flex items-center gap-6">
                <a href="/" class="text-gray-100 font-semibold hover:text-white">Védett fajok</a>
                <a href="/crud/list" class="text-gray-300 hover:text-white hover:bg-white/5 px-3 py-2 rounded-md">Adatbázis</a>
            </div>

            <div class="flex items-center gap-6">

                {{#if user}}
                    <span class="text-gray-300">
                      Bejelentkezve: <strong class="text-white">{{user.username}}</strong>
                    </span>

                    {{#if (eq user.role "admin")}}
                        <a href="/crud/admin"
                           class="text-indigo-300 hover:text-white hover:bg-white/5 px-3 py-2 rounded-md">
                            Admin
                        </a>
                    {{/if}}

                    <a href="/logout" class="text-red-400 hover:text-white hover:bg-red-500/20 px-3 py-2 rounded-md">
                        Kijelentkezés
                    </a>

                {{else}}
                    <a href="/login" class="text-gray-300 hover:text-white hover:bg-white/5 px-3 py-2 rounded-md">
                        Bejelentkezés
                    </a>

                    <a href="/register" class="text-gray-300 hover:text-white hover:bg-white/5 px-3 py-2 rounded-md">
                        Regisztráció
                    </a>
                {{/if}}

            </div>

        </div>
    </div>
</nav>
