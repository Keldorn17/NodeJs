<nav class="sticky top-0 z-50 w-full bg-white/80 dark:bg-gray-900/80 backdrop-blur-md border-b border-gray-200 dark:border-gray-700 transition-colors duration-300">
    <div class="mx-auto px-4">
        <div class="flex h-16 items-center justify-between">

            <div class="flex items-center gap-6">
                <a href="/" class="text-emerald-600 dark:text-emerald-400 font-bold text-xl flex items-center gap-2">
                    <span>🌿</span> Védett fajok
                </a>

                {{#if user}}
                    <div class="hidden md:flex gap-6">
                        <a href="/animals"
                           class="px-1 py-2 border-b-2 font-medium transition-colors duration-200
                           {{#if (eq activePage 'animals')}}
                               border-emerald-600 text-emerald-600 dark:border-emerald-400 dark:text-emerald-400
                           {{else}}
                               border-transparent text-gray-600 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-white hover:border-emerald-200 dark:hover:border-gray-700
                           {{/if}}">
                            Adatbázis
                        </a>
                    {{#if (eq user.role 'admin')}}
                        <a href="/admin"
                           class="px-1 py-2 border-b-2 font-medium transition-colors duration-200
                           {{#if (eq activePage 'admin')}}
                               border-emerald-600 text-emerald-600 dark:border-emerald-400 dark:text-emerald-400
                           {{else}}
                               border-transparent text-gray-600 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-white hover:border-emerald-200 dark:hover:border-gray-700
                           {{/if}}">
                            Kategóriák
                        </a>

                        <a href="/contact-list"
                           class="px-1 py-2 border-b-2 font-medium transition-colors duration-200
                           {{#if (eq activePage 'contact')}}
                               border-emerald-600 text-emerald-600 dark:border-emerald-400 dark:text-emerald-400
                           {{else}}
                               border-transparent text-gray-600 dark:text-gray-300 hover:text-emerald-600 dark:hover:text-white hover:border-emerald-200 dark:hover:border-gray-700
                           {{/if}}">
                            Üzenetek
                        </a>
                    {{/if}}
                    </div>
                {{/if}}
            </div>

            <div class="flex items-center gap-4">

                {{#if user}}
                    <div class="hidden md:flex flex-col items-end leading-tight">
                        <span class="text-xs text-gray-500 dark:text-gray-400">Bejelentkezve</span>
                        <strong class="text-gray-800 dark:text-white">{{user.username}}</strong>
                    </div>

                    <a href="/auth/logout" class="hidden md:inline-flex text-red-600 dark:text-red-400 border border-red-200 dark:border-red-900 hover:bg-red-50 dark:hover:bg-red-900/20 px-3 py-2 rounded-md font-medium transition text-sm">
                        Kijelentkezés
                    </a>
                {{else}}
                    <div class="hidden md:flex gap-4">
                        <a href="/auth/login" class="text-gray-700 dark:text-gray-200 hover:text-emerald-600 px-4 py-2 dark:hover:text-emerald-400 font-medium transition">
                            Bejelentkezés
                        </a>
                        <a href="/auth/register" class="bg-emerald-600 hover:bg-emerald-700 text-white px-4 py-2 rounded-md font-medium transition shadow-lg shadow-emerald-600/20">
                            Regisztráció
                        </a>
                    </div>
                {{/if}}

                <button id="mobile-menu-btn" type="button" class="md:hidden inline-flex items-center justify-center p-2 rounded-md text-gray-700 dark:text-gray-200 hover:text-emerald-600 dark:hover:text-white focus:outline-none">
                    <span class="sr-only">Menü megnyitása</span>
                    <svg id="menu-icon-closed" class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                    <svg id="menu-icon-open" class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
        </div>
    </div>

    <div id="mobile-menu" class="hidden md:hidden border-t border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-900">
        <div class="px-2 pt-2 pb-3 space-y-1">

            {{#if user}}
                <div class="px-3 py-3 border-b border-gray-100 dark:border-gray-800 mb-2">
                    <div class="text-base font-medium text-gray-800 dark:text-white">{{user.username}}</div>
                    <div class="text-sm font-medium text-gray-500 dark:text-gray-400">Bejelentkezve</div>
                </div>

                <a href="/animals"
                   class="block px-3 py-2 rounded-md text-base font-medium
                   {{#if (eq activePage 'animals')}}
                       bg-emerald-50 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400
                   {{else}}
                       text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800 hover:text-emerald-600
                   {{/if}}">
                    Adatbázis
                </a>

                <a href="/admin"
                   class="block px-3 py-2 rounded-md text-base font-medium
                   {{#if (eq activePage 'admin')}}
                       bg-emerald-50 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400
                   {{else}}
                       text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800 hover:text-emerald-600
                   {{/if}}">
                    Kategóriák
                </a>

                <a href="/contact-list"
                   class="block px-3 py-2 rounded-md text-base font-medium
                   {{#if (eq activePage 'contact')}}
                       bg-emerald-50 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-400
                   {{else}}
                       text-gray-700 dark:text-gray-300 hover:bg-gray-50 dark:hover:bg-gray-800 hover:text-emerald-600
                   {{/if}}">
                    Üzenetek
                </a>

                <a href="/auth/logout" class="block mt-4 px-3 py-2 rounded-md text-base font-medium text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20">
                    Kijelentkezés
                </a>
            {{else}}
                <a href="/auth/login" class="block px-3 py-2 rounded-md text-base font-medium text-gray-700 dark:text-gray-300 hover:text-emerald-600 hover:bg-gray-50 dark:hover:bg-gray-800">
                    Bejelentkezés
                </a>
                <a href="/auth/register" class="block px-3 py-2 rounded-md text-base font-medium text-emerald-600 font-bold hover:bg-gray-50 dark:hover:bg-gray-800">
                    Regisztráció
                </a>
            {{/if}}
        </div>
    </div>
</nav>

<script>
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');
    const iconClosed = document.getElementById('menu-icon-closed');
    const iconOpen = document.getElementById('menu-icon-open');

    btn.addEventListener('click', () => {
        menu.classList.toggle('hidden');
        iconClosed.classList.toggle('hidden');
        iconOpen.classList.toggle('hidden');
    });
</script>